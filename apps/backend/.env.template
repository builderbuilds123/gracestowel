# .env.template for development

# PostgreSQL (Railway Dev external URL)
DATABASE_URL="postgresql://<POSTGRES_DEV_USER>:<POSTGRES_DEV_PASSWORD>@<POSTGRES_DEV_HOST>:<POSTGRES_DEV_PORT>/railway"

# Redis (Railway Dev external URL)
REDIS_URL="redis://<REDIS_DEV_USER>:<REDIS_DEV_PASSWORD>@<REDIS_DEV_HOST>:<REDIS_DEV_PORT>"

# CORS for local dev
STORE_CORS="http://localhost:5173"
ADMIN_CORS="http://localhost:7001"
AUTH_CORS="http://localhost:5173"

# Secrets (dev placeholders)
JWT_SECRET="dev-secret"
COOKIE_SECRET="dev-secret"
